export type TFonts = {
    body: string,
    heading: string,
    monospace: string,
};

export type TFontWeights = {
    regular: number,
    medium: number,
    bold: number,
};

export type TFontSizes = {
    caption: string,
    button: string,
    body: string,
    title: string,
    h5?: string,
    h4?: string,
    h3?: string,
    h2?: string,
    h1?: string,
};
